/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "127.0.0.1",
      "localhost",
      "api.example.com",
      "via.placeholder.com",
      "www.apis.bisharh.com",
    ],
  },
};

module.exports = nextConfig;
